import React, {Component} from 'react'
import {
  AppRegistry
} from 'react-native'

var examplefiredata = require('./appcode/App');

AppRegistry.registerComponent('examplefiredata', () => examplefiredata);
